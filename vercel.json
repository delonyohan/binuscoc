{
  "version": 2,
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build"
    },
    {
      "src": "backend/api/main.py",
      "use": "@vercel/python",
      "config": {
        "build": "pip install -r requirements.txt && python -c \"import ultralytics; import os; from pathlib import Path; p = Path(ultralytics.__file__).parent; print('Cleaning up ultralytics package...'); [f.unlink() for f in p.glob('*.pt') if f.is_file()]; print('Cleanup complete.')\""
      }
    }
  ]
}